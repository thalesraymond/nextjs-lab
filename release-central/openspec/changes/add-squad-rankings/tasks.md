1. **Mock Data Generation:** Create mock data for squads, including historical positions (to calculate delta), release train, community, points, deliveries, and detailed lists of delivery items and events.
2. **Tier Calculation logic:** Implement a utility function to calculate and assign tiers (1 to 5) to squads based on the defined percentage distribution (15%, 20%, 30%, 20%, 15%).
3. **Rankings Table Component:** Create a UI component (`SquadRankingsTable`) to display the list of squads with all required columns: Position, Delta (with up/down/equal icons), Squad Name, Release Train, Community, Total Points, Total Deliveries, and Tier. Ensure all text is in pt-br.
4. **Squad Details Component:** Create a Side Panel (Sheet) component (`SquadDetailsSheet`) to display the detailed metrics (Total Deliveries, Events, Score) and lists of delivery items and events for a selected squad. Ensure all text is in pt-br.
5. **Dashboard Integration:** Integrate `SquadRankingsTable` and `SquadDetailsSheet` into `release-central/app/game/page.tsx` below the existing components, and wire up the selection state.
