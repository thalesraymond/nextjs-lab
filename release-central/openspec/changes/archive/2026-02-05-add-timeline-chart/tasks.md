## 1. Preparation
- [x] 1.1 Install `recharts` dependency: `npm install recharts`.

## 2. Implementation - Data
- [x] 2.1 Create `app/game/utils/mock-generator.ts` with a function `generateGameStats` that produces random squads, deliveries, and events.
- [x] 2.2 Update `app/game/data.ts` to export data generated by this utility (e.g., `export const mockGameStats = generateGameStats({ ... })`).

## 3. Implementation - UI
- [x] 3.1 Create `app/game/_components/timeline-chart.tsx`.
    - Accept `GameStats` or `SquadScore[]` as props.
    - Process data: Aggregate deliveries and events by date (YYYY-MM-DD).
    - Render a Recharts `BarChart` (or `ComposedChart`) showing delivery counts per day.
    - Add tooltips to show event details for the day.
- [x] 3.2 Update `app/game/page.tsx` to include the `TimelineChart` component below the KPI header.

## 4. Verification
- [x] 4.1 Verify the page loads and the chart displays data.
- [x] 4.2 Verify the mock data varies (or is at least substantial enough to look good) based on the generator settings.
- [x] 4.3 Check that hovering over the chart shows correct delivery/event counts.
